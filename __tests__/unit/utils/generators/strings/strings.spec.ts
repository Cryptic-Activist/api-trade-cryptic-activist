// import {
//   generateRandomNames,
//   slugifyString,
//   slugifyStringLowerCase,
//   generateRandonAdjective,
//   generateUniqueUsername,
// } from '@utils/generators/strings/strings';

describe('Strings generators', () => {
  it('should generate random names', () => {
    // const randomNames: string[] = generateRandomNames();
    // expect(typeof randomNames).toBe('object');
    // expect(randomNames.length).toBe(2);
    // expect(typeof randomNames[0]).toBe('string');
    // expect(typeof randomNames[1]).toBe('string');
  });

  it('should generate a slug', () => {
    // const slug: string = slugifyString('Jest Testing');
    // expect(slug).toBe('Jest-Testing');
  });

  it('should generate a lowercased slug', () => {
    // const slug: string = slugifyStringLowerCase('Jest Testing');
    // expect(slug).toBe('jest-testing');
  });

  it('should generate a random adjective string', () => {
    // const adjective: string = generateRandonAdjective();
    // expect(typeof adjective).toBe('string');
    // expect(adjective.length).toBeGreaterThan(0);
  });

  it('should generate a random unique username string', () => {
    // const username: string = generateUniqueUsername('tseting-tester');
    // const usernameArr: string[] = username.split('-');
    // expect(typeof username).toBe('string');
    // expect(typeof usernameArr).toBe('object');
    // expect(usernameArr.length).toBe(3);
    // usernameArr.forEach((un) => {
    //   expect(typeof un).toBe('string');
    //   expect(un.length).toBeGreaterThan(0);
    // });
  });
});
