// import { generateAccessToken } from '@utils/generators/jwt/jwt';

describe('Json Web Token', () => {
  it('should generate a json web token access token', () => {
    // const accessToken: string = generateAccessToken('testId');
    // console.log('accessToken:', accessToken);
    // expect(typeof accessToken).toBe('boolean');
  });
});
