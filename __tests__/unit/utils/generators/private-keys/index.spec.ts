// import { generatePrivateKeys } from '@utils/generators/private-keys';

describe('Private Keys Generators', () => {
  it('should generate private keys', async () => {
    // const privateKeysArr: string[] = await generatePrivateKeys();
    // expect(typeof privateKeysArr).toBe('object');
    // privateKeysArr.forEach((key) => {
    //   expect(typeof key).toBe('string');
    //   expect(key.length).toBeGreaterThan(0);
    // });
    expect(true).toBeTruthy();
  });
});
