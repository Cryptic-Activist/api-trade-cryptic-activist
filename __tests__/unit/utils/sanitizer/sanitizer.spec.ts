// import {
//   sanitizeInputRegistration,
//   sanitizeInputLanguage,
// } from '@utils/sanitizer/index';

describe('Html sanitizer', () => {
  it('should sanitize registration coming input data', () => {
    //   const sanitizedData = sanitizeInputRegistration(
    //     {
    //       firstName: '<h1>First</h1>',
    //       lastName: '<p>Last</p>',
    //     },
    //     '<script>alert("test")Hello</script>test@test.com',
    //   );
    //   expect(typeof sanitizedData).toBe('object');
    //   expect(typeof sanitizedData.names).toBe('object');
    //   expect(typeof sanitizedData.names.firstName).toBe('string');
    //   expect(typeof sanitizedData.names.lastName).toBe('string');
    //   expect(typeof sanitizedData.username).toBe('string');
    // });
    // it('should sanitize language name input data', () => {
    //   const sanitizedData = sanitizeInputLanguage('<a> Ara#bic</a> ');
    //   expect(typeof sanitizedData).toBe('object');
    //   expect(typeof sanitizedData.languageName).toBe('string');
    //   expect(sanitizedData.languageName).toBe('Arabic');
  });
});
